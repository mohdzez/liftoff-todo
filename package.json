{
  "name": "liftoff-todo",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny single-service todo app, built to deploy cleanly on Liftoff.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
